[project]
name = "instagram-mcp"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.0",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
]
